import util
import statistics
# import market class
import Market

# Given a market state, output a fair value, 

def fair_value(stock):
    # compute halfway point between best bid and best offer
    best_bid = max(stock[
    best_offer = 
    return statistics.median([best_bid, best_offer])

def strategy(fair_val):
    # compute 
        
        

def next_action(market):
    # takes in a market, computes a fair value, outputs some action based on strategy
    for stock in market.stocks
